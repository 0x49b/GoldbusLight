# 🛠️ Development Insights

> _Generated by Gemini with google/gemini-1.5-pro on 2026-05-20._

This section provides insights into the development aspects of the Goldbus Light Controller, covering design decisions, testing strategies, and a guide for local development.

## Design Decisions

-   **Wails Framework:** The choice of Wails allows for rapid development of a cross-platform desktop application using familiar web technologies (React/TypeScript) for the UI, while leveraging Go's performance and system-level access for the backend. This avoids the complexities of Electron while providing a native look and feel.
-   **Centralized Backend Controller:** The `internal/controller` package acts as the single source of truth for the application's state and orchestrates all major functionalities. This promotes a clear separation of concerns and simplifies state management.
-   **Reactive Frontend:** The React frontend, combined with Zustand for state management and Wails events, creates a reactive UI that efficiently reflects changes from the backend.
-   **Modular Go Backend:** The `internal/` package is structured into distinct modules (e.g., `wled`, `dmx`, `audio`, `network`) to promote maintainability and allow for independent development and testing of subsystems.
-   **Future-Proofing (KMP Migration):** The existence of the `gemini-kmp.md` migration plan indicates a strategic decision to move towards Kotlin Multiplatform. This suggests that future development may eventually transition to a unified Kotlin codebase across the entire application stack, which could impact current design decisions by favoring patterns easily transferable to Kotlin.

## Testing

### Go Backend

-   **Unit Tests:** Go backend packages include unit tests, typically found in `_test.go` files alongside the source code (e.g., `internal/audio/features_test.go`, `internal/controller/controller_dmx_simulator_test.go`, `internal/controller/dmx_party_test.go`, `internal/wledhttp/host_test.go`). These tests verify the correctness of individual functions and components.
-   **Integration Tests:** While not explicitly detailed in a separate framework, the simulation flags (`simulateWled`, `simulateUsbDmx`, `simulateArtNet`) mentioned in the `claude-kmp.md` (and present in `frontend/src/hooks/useControllerApp.ts` and `frontend/src/components/settings/ControllerSettingsView.tsx`) suggest a capability for running the application in a simulated environment to test interactions between components without requiring physical hardware.

### React Frontend

-   Standard React testing practices (e.g., using React Testing Library, Jest) would typically be employed, although specific test files are not immediately visible in the provided file listing.

## Local Development Guide

To set up and run the Goldbus Light Controller for local development, follow these general steps:

### Prerequisites

-   **Go:** Ensure Go (version 1.21 or later, as per `go.mod`) is installed and configured on your system.
-   **Node.js & npm/Yarn:** Install Node.js (version 16 or later, inferred from typical React project setups) and a package manager like npm or Yarn.
-   **Wails CLI:** Install the Wails CLI as per the official Wails documentation.

### Backend Setup (Go)

1.  **Navigate to Project Root:** Open your terminal and change the directory to the project root (`/home/florian/GolandProjects/GoldbusLight`).
2.  **Install Go Dependencies:**
    ```bash
    go mod tidy
    ```

### Frontend Setup (React/TypeScript)

1.  **Navigate to Frontend Directory:**
    ```bash
    cd frontend/
    ```
2.  **Install Node.js Dependencies:**
    ```bash
    npm install # or yarn install
    ```
3.  **Return to Project Root:**
    ```bash
    cd ..
    ```

### Running the Application

The project uses `Taskfile.yml` for defining common commands.

1.  **Develop Mode (Frontend hot-reloading):**
    ```bash
    task dev
    ```
    This command typically starts the Wails application in development mode, enabling live-reloading for frontend changes.

2.  **Build Production Application:**
    ```bash
    task build
    ```
    This command compiles the Go backend and bundles the React frontend into a single executable using Wails.

### Key `Taskfile.yml` Commands (Examples)

Examine `Taskfile.yml` for specific build, run, and utility commands. Common tasks might include:

-   `task frontend:dev`: Runs the frontend in development mode.
-   `task build:release`: Builds a production-ready release.
-   `task tidy`: Cleans up Go modules.

### Wails Bindings

Wails automatically generates TypeScript bindings for the Go backend services. These are found in `frontend/src/bindings/`. Do **not** manually edit these files as they are regenerated during the build process.

### Environment-Specific Setup

-   For testing features related to network configuration, ensure your operating system has `nmcli` (Linux), `networksetup` (macOS), or `netsh` (Windows) available and accessible with necessary permissions.
-   For USB-DMX serial communication, ensure required drivers are installed for your specific USB-to-DMX adapter.
