# 🔐 Infrastructure & Setup

> _Generated by Gemini with google/gemini-1.5-pro on 2026-05-20._

This section outlines critical infrastructure considerations and setup procedures for the Goldbus Light Controller, focusing on environment variables and deployment to a Raspberry Pi.

## Environment Variables

The application can be configured using environment variables, particularly for logging and display modes.

| Variable             | Description                                          | Example Value         |
| :------------------- | :--------------------------------------------------- | :-------------------- |
| `GOLDBUS_FULLSCREEN` | If set to `1`, the application launches in fullscreen. | `1`                   |
| `GOLDBUS_LOG_FILE`   | Specifies the path for the application log file.     | `/var/log/goldbus.log` |

## Deployment & Setup

### Raspberry Pi Installation

The project includes specific scripts for installing the application on a Raspberry Pi, indicating an ARM64 Linux target.

-   **`scripts/install-raspberry-pi.sh`**: This script is designed for initial setup and installation on a Raspberry Pi. It likely handles dependencies, service configuration, and application placement.
-   **`scripts/install-release.sh`**: Used for installing specific releases, typically taking a version tag as an argument.

### Containerization

The presence of `build/docker/Dockerfile.cross` and `build/docker/Dockerfile.server` suggests that the application can be containerized, potentially for CI/CD pipelines or server-side deployments, though the primary target appears to be a desktop application.

### Network Configuration

The backend (`internal/network/`) interacts with system-level network configuration tools for managing Wi-Fi and access points. This is platform-dependent:

-   **Linux:** `nmcli` (NetworkManager CLI)
-   **macOS:** `networksetup`
-   **Windows:** `netsh`

These tools require appropriate system permissions for the application to function correctly.

## External Dependencies

-   **WLED Devices:** The application communicates with physical WLED devices over the network (HTTP/JSON).
-   **DMX Hardware:** External USB-to-DMX serial interfaces and/or Art-Net nodes for DMX lighting control.
-   **Audio Input:** System audio input devices (microphones, loopback devices) for "Party Mode" audio reactivity.

The application expects these external systems to be available and correctly configured for full functionality.
