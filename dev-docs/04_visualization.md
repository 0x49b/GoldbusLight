# 📊 System Visualization

> _Generated by Gemini with google/gemini-1.5-pro on 2026-05-20._

Detailed system visualizations (Sequence Diagrams, Class Diagrams, Package Diagrams) require deeper static analysis and specialized tooling for automatic generation from Go and TypeScript codebases. While these are invaluable for understanding complex interactions, they are not automatically produced by this documentation generation process.

However, should you wish to generate them manually or with specific tools, here's an overview of what would be valuable:

-   **Sequence Diagram (Frontend Action to Backend Response):** Illustrates the call flow from a user action in the React UI, through the Wails bridge, into the Go backend service, down to the core controller logic, and back to the UI via state updates.
-   **Class Diagram (Go Backend):** Shows the relationships between key Go structs and interfaces in `internal/controller`, `internal/dmx`, `internal/wled`, etc., including their methods and properties.
-   **Package Diagram (Go Backend):** Visualizes the dependencies between the Go `internal/` packages.
-   **Data Flow Pipeline Diagram:** Focuses on how data (e.g., DMX values, WLED state changes, audio features) flows between different components and subsystems in both the frontend and backend.

These diagrams would significantly enhance the understanding of the system's runtime behavior and internal structure.
